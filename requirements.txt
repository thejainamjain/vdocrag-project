# Core
torch>=2.2.0
transformers>=4.45.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0        # 4-bit/8-bit quantization for LoRA on small GPUs
datasets>=2.20.0
huggingface_hub>=0.24.0

# Image handling
Pillow>=10.0.0
qwen-vl-utils>=0.0.8        # helper for Qwen2-VL image/video preprocessing

# Retrieval / eval
faiss-cpu>=1.8.0            # swap for faiss-gpu on the RTX machine if desired
scikit-learn>=1.4.0
pytrec_eval>=0.5            # Recall@k, nDCG for retrieval eval

# Training utils
deepspeed>=0.14.0            # optional, for the RTX box if multi-step training grows
wandb>=0.17.0                 # experiment tracking (optional but recommended)

# Dev
pytest>=8.0.0
jupyter>=1.0.0
tqdm>=4.66.0
